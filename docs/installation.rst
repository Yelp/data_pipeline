============
Installation
============

At the command line either via pip::

    $ pip install data_pipeline

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv data_pipeline
    $ pip install data_pipeline
